export class Parameters {
    limit: string; 
    sort_by_field: string; 
    sort_by_order: string;
    skip: string;
    where_id: string;
    where_Name: string;
    where_City: string;
    where_Date: string;
    where_Price: string;
    where_Promote: string;
    where_AvaiableNumberOfPlaces: string;
}

